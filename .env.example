# Example environment variables for yourlicensure-mvp
# Copy this file to `.env` and fill with your own secret values.
# DO NOT commit `.env` or real credentials to source control.

# Google OAuth2 client ID (from Google Cloud Console)
CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Google OAuth2 client secret (from Google Cloud Console)
CLIENT_SECRET=your-google-client-secret

# Redirect URI registered for the OAuth2 client.
# Common local value: http://localhost:3000 (or http://localhost)
# Must exactly match a redirect URI registered for the client above.
REDIRECT_URI=http://localhost:3000

# The app stores the OAuth token to `token.json` by default.
# If you want to change that path, edit `auth.js` or set a different name here and
# update `auth.js` accordingly (no env var is currently used for token path).
